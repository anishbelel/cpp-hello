#include<bits/stdc++.h>
using namespace std;

// #include <ext/pb_ds/assoc_container.hpp>
// #include <ext/pb_ds/tree_policy.hpp>
// using namespace __gnu_pbds;
// typedef tree<int, null_type, less<int>, rb_tree_tag, tree_order_statistics_node_update> ordered_set; //*a.find_by_order(x) a.order_of_key(x) a.insert(x) a.erase(x) 
// typedef tree<int, null_type, greater<int>, rb_tree_tag, tree_order_statistics_node_update> ordered_set1;

#define int long long
#define fr(i,n) for(int i=0;i<n;i++)
#define frr(i,n) for(int i=n;i>=0;i--)
typedef pair<int,int>pr;
typedef vector<int>vii;
typedef vector<pr>vip;
typedef map<int,int> maa;
#define co cout <<
#define en << endl
#define sor(vec) sort(vec.begin(),vec.end())
#define rsor(vec) sort(vec.begin(),vec.end(),greater<int>())
#define upp(vec,idx,x) upper_bound(vec.begin()+idx,vec.end(),x)-vec.begin()

int n, k;

void solve(){

    cin >> n >> k;
    vector<int> vec(n);
    for(int i = 0; i < n; i++) cin >> vec[i];
    
}



signed main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);cout.tie(NULL);

    int t;cin >> t;while(t--)
    solve();
}